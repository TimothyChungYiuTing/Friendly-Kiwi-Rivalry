using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class Canvas0 : MonoBehaviour
{
    public TextMeshProUGUI Text_Dist;

    public float resultDist = -999f;
    public int ID;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
